#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int f,n,q;
    cin >> f >> n;
    vector<int> fert;
    int r;
    for (int i = 0; i < n*2; i++){
        cin >> r;
        fert[i] = r;
    }
    cin >> q;
    int l,r;
    for (int i = 0; i < q; i++){
        
    }
    return 0;
}